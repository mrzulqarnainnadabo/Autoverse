# Vercel Auto-Deploy Trigger

This file exists to record the production deployment trigger for the AUTOVERSE backend.

Vercel is connected to the repository's `main` branch, so pushes to `main` trigger a new deployment.
